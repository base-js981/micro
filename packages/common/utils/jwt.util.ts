// JWT utilities
// Note: Install jsonwebtoken package: npm install jsonwebtoken @types/jsonwebtoken
// import * as jwt from 'jsonwebtoken';
// import { JwtClaims } from '../types/jwt-claims.interface';

export class JwtUtil {
  // TODO: Implement with jsonwebtoken package
  // static sign(payload: JwtClaims, secret: string, expiresIn?: string): string {
  //   return jwt.sign(payload, secret, expiresIn ? { expiresIn } : {});
  // }

  // static verify(token: string, secret: string): JwtClaims {
  //   return jwt.verify(token, secret) as JwtClaims;
  // }

  // static decode(token: string): JwtClaims | null {
  //   try {
  //     return jwt.decode(token) as JwtClaims;
  //   } catch {
  //     return null;
  //   }
  // }
}

