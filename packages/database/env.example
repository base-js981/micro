# Database Configuration
# Format: oracle://user:password@host:port/service_name
# For local development (outside Docker): use localhost
# For Docker containers: use service name (oracle)
DATABASE_URL=oracle://micro_app:password@localhost:1521/XEPDB1

# Environment
NODE_ENV=development
